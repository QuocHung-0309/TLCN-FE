"use client";
import SlideshowLayout from "@/components/ui/SlideshowLayout";

export default function RegisterOtpLayoutClient({ children }: { children: React.ReactNode }) {
  return <SlideshowLayout>{children}</SlideshowLayout>; // Hình bên phải
}
